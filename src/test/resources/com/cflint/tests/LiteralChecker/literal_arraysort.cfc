component {
    function foo() {
        arraySort(bar, "numeric", 'ASC');
        arraySort(bar, "numeric", 'ASC');
        arraySort(bar, "numeric", 'ASC');
        arraySort(bar, "numeric", 'ASC');
        arraySort(bar, "numeric", 'ASC');
        arraySort(bar, "numeric", 'ASC');
    }
}